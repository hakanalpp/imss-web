import { SubmitFormResultDto } from './SubmitFormResultDto';

describe('SubmitFormResultDto', () => {
  it('should be defined', () => {
    //expect(new SubmitFormResultDto()).toBeDefined();
  });
});
